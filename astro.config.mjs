import { defineConfig } from 'astro/config';

export default defineConfig({
  // If your repo is username.github.io, keep this as your domain.
  // If your repo is personal-site, change site and add: base: '/personal-site'
  site: 'https://your-github-username.github.io'
});
